apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "CLOPS-helm.fullname" . }}-test-connection"
  labels:
    {{- include "CLOPS-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "CLOPS-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
