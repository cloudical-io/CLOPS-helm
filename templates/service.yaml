apiVersion: v1
kind: Service
metadata:
  name: {{ include "CLOPS-helm.fullname" . }}
  labels:
    {{- include "CLOPS-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "CLOPS-helm.selectorLabels" . | nindent 4 }}
